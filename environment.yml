# Conda: conda env create -f environment.yml && conda activate capstone-biomed
name: capstone-biomed
channels:
  - conda-forge
dependencies:
  - python>=3.10,<3.13
  - pip
  - pip:
      - -r requirements.txt
